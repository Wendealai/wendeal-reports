# Build-time environment for Docker
# This prevents database connection errors during Docker build
DATABASE_URL=postgresql://build:build@localhost:5432/build_db?sslmode=disable
DIRECT_URL=postgresql://build:build@localhost:5432/build_db?sslmode=disable
NODE_ENV=production
NEXTAUTH_SECRET=build-secret
NEXTAUTH_URL=http://localhost:3000
